function runActivity() {
  let user_number;
  let integer;
  let decimal;
  let even;
  let odd;
  let negative;
  let positive;

  let user_input = Number(prompt("Choose a number between 2 to 20"));
  for (let i = 0; i < user_input; i++) {
    if (user_input > 2 && user_input < 20) {
      user_number = Number(prompt("Give me a number."));
    }
    i = integer;
  }
}
